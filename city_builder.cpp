#include <iostream>
#include <SFML/Graphics.hpp>

int main(int argc, char* argv[]) {
    bool isRunning = true;

    //initialization function
    //initializeCity();
        //Each saved game will be referred to as a "City"
    
    std::cout << "Setting things up";

    while(isRunning) {
        //Process input func


        //Update game state func


        //Render the game func
        //This step involves rendering the current game state and displaying it on the screen. 
        //It includes drawing the game world, characters, objects, UI elements, and any other visual components. 
        //This process often utilizes graphics libraries to handle rendering operations efficiently.
    }

    //Cleanup func

    return 1;
}